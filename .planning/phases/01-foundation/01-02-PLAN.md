---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .gitignore
autonomous: false
requirements:
  - INFR-01
  - INFR-02
  - INFR-03

user_setup:
  - service: github
    why: "Host source code and trigger Cloudflare Pages auto-deploy"
    env_vars: []
    dashboard_config:
      - task: "Authenticate gh CLI if not already authenticated"
        location: "Terminal: gh auth login"
  - service: cloudflare-pages
    why: "Static site hosting for SPA"
    env_vars: []
    dashboard_config:
      - task: "Have Cloudflare account ready (free tier)"
        location: "https://dash.cloudflare.com"
      - task: "Configure custom domain twittercelebrity.com after initial deploy"
        location: "Cloudflare Pages project > Custom domains"

must_haves:
  truths:
    - "Visiting the Cloudflare Pages URL loads the React Router 7 app without blank screen or error"
    - "Direct URL access to a non-root path (e.g., /test-path) does NOT return a Cloudflare 404 — SPA routing serves index.html"
    - "twittercelebrity.com resolves to the deployed site (after custom domain configuration)"
  artifacts:
    - path: "public/_redirects"
      provides: "SPA routing fallback"
      contains: "/* /index.html 200"
  key_links:
    - from: "GitHub repo dchadcluff/twittercelebrity"
      to: "Cloudflare Pages project"
      via: "Git integration auto-deploy"
      pattern: "push to main triggers build"
    - from: "Cloudflare Pages project"
      to: "twittercelebrity.com"
      via: "Custom domain DNS"
      pattern: "CNAME or CF DNS record"
---

<objective>
Deploy the scaffolded React Router 7 SPA to Cloudflare Pages via GitHub integration and verify SPA routing works on the live domain.

Purpose: Validate the deployment pipeline end-to-end before any feature work begins. This is the highest-risk item in Phase 1 — if SPA routing is misconfigured, it only surfaces in production, not local dev.
Output: Live site at Cloudflare Pages URL (and twittercelebrity.com after custom domain config) serving the RR7 SPA with working client-side routing.
</objective>

<execution_context>
@/Users/chad.cluff/personal/twittercelebrity/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chad.cluff/personal/twittercelebrity/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub repo, initialize git, push code, and connect Cloudflare Pages</name>
  <files>.gitignore</files>
  <read_first>
    .planning/phases/01-foundation/01-CONTEXT.md (deploy workflow decisions: github.com/dchadcluff/twittercelebrity, Cloudflare Pages git integration, build output build/client/)
    .planning/phases/01-foundation/01-RESEARCH.md (SPA routing pitfalls, _redirects verification)
    .gitignore (if it exists after scaffold — check contents)
  </read_first>
  <action>
1. Initialize git in the project root (if not already a git repo):
   ```
   git init
   ```

2. Create a `.gitignore` if one doesn't exist, ensuring it includes:
   ```
   node_modules/
   build/
   .cache/
   .env
   .env.local
   ```

3. Create the GitHub repository using the gh CLI:
   ```
   gh repo create dchadcluff/twittercelebrity --public --source=. --remote=origin
   ```
   If the repo already exists, just add the remote:
   ```
   git remote add origin https://github.com/dchadcluff/twittercelebrity.git
   ```

4. Stage all files, create initial commit, and push:
   ```
   git add -A
   git commit -m "feat: scaffold RR7 SPA with celebrity data and cyberpunk tokens"
   git branch -M main
   git push -u origin main
   ```

5. Connect to Cloudflare Pages. Use the Wrangler CLI if available:
   ```
   npx wrangler pages project create twittercelebrity --production-branch=main
   ```

   If wrangler is not available or auth is needed, provide instructions for the checkpoint.

   Configure build settings for Cloudflare Pages:
   - **Build command:** `npm run build`
   - **Build output directory:** `build/client`
   - **Root directory:** `/` (project root)
   - **Node.js version:** 20 or higher

6. If using wrangler CLI deploy:
   ```
   npm run build && npx wrangler pages deploy build/client --project-name=twittercelebrity
   ```

7. After deploy, note the Cloudflare Pages URL (e.g., `twittercelebrity.pages.dev`) for verification in the next task.
  </action>
  <verify>
    <automated>cd /Users/chad.cluff/personal/twittercelebrity && git remote -v | grep "dchadcluff/twittercelebrity" && echo "REMOTE OK"</automated>
  </verify>
  <acceptance_criteria>
    - git remote -v shows origin pointing to github.com/dchadcluff/twittercelebrity
    - git log shows at least one commit on main branch
    - GitHub repo dchadcluff/twittercelebrity exists and contains the pushed code
  </acceptance_criteria>
  <done>Code pushed to GitHub repo dchadcluff/twittercelebrity, Cloudflare Pages deployment initiated</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify live deployment and configure custom domain</name>
  <files>N/A - verification only</files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md (SPA routing pitfall: 404 on direct URL access)
  </read_first>
  <action>
    This is a human verification checkpoint. Claude has deployed the site in Task 1. The user now verifies the live deployment works correctly and configures the custom domain via the Cloudflare dashboard.
  </action>
  <what-built>
    React Router 7 SPA deployed to Cloudflare Pages with SPA routing fallback via _redirects.
  </what-built>
  <how-to-verify>
    1. Visit the Cloudflare Pages URL (e.g., https://twittercelebrity.pages.dev)
       - EXPECTED: Page loads showing "Twitter Celebrity" heading in neon cyan on dark background
       - FAIL IF: Blank white page, Cloudflare error page, or JavaScript console errors

    2. Visit a non-root URL directly (e.g., https://twittercelebrity.pages.dev/test-anything)
       - EXPECTED: Page loads the same SPA content (React Router handles routing client-side)
       - FAIL IF: Cloudflare 404 page appears — this means _redirects is not working

    3. Open browser DevTools > Network tab, hard refresh the root URL
       - EXPECTED: index.html served with 200 status, JS bundles load, no 404s for assets
       - FAIL IF: Any critical asset returns 404

    4. Configure custom domain (manual step):
       - Go to Cloudflare Pages dashboard > twittercelebrity project > Custom domains
       - Add "twittercelebrity.com" as a custom domain
       - Follow DNS configuration prompts (if domain is on Cloudflare DNS, it auto-configures; if external, add CNAME record pointing to twittercelebrity.pages.dev)
       - Wait for DNS propagation (usually 1-5 minutes on Cloudflare DNS, up to 24h for external)

    5. After custom domain propagates, visit https://twittercelebrity.com
       - EXPECTED: Same SPA loads as from the .pages.dev URL
       - FAIL IF: DNS error, certificate error, or wrong content
  </how-to-verify>
  <verify>
    <automated>echo "Human verification checkpoint — no automated command"</automated>
  </verify>
  <acceptance_criteria>
    - Cloudflare Pages URL (twittercelebrity.pages.dev) loads without blank screen or errors
    - Non-root URL (e.g., /test-anything) does NOT return Cloudflare 404
    - twittercelebrity.com resolves to the deployed site
  </acceptance_criteria>
  <done>Live site verified at Cloudflare Pages URL and twittercelebrity.com, SPA routing confirmed working on direct URL access</done>
  <resume-signal>Type "approved" if all 5 checks pass, or describe which check failed and what you saw</resume-signal>
</task>

</tasks>

<verification>
1. GitHub repo exists at github.com/dchadcluff/twittercelebrity with code pushed
2. Cloudflare Pages URL loads the RR7 SPA without errors
3. Direct URL access to non-root paths serves the SPA (not a 404)
4. twittercelebrity.com resolves to the deployed site (after custom domain setup)
</verification>

<success_criteria>
- Live site accessible at Cloudflare Pages URL
- SPA routing works (direct URL access returns the app, not 404)
- Custom domain twittercelebrity.com configured and serving the site
- No blank screens, no JS errors, no broken asset loads
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
