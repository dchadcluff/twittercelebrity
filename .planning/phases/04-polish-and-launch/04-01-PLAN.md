---
phase: 04-polish-and-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/styles/app.css
  - app/components/StickyHeader.tsx
  - app/components/CelebrityCard.tsx
  - app/routes/home.tsx
autonomous: true
requirements:
  - DESG-02
  - DESG-03

must_haves:
  truths:
    - "The StickyHeader title 'Twitter Celebrity' displays a visible CSS glitch animation (chromatic aberration flicker)"
    - "Celebrity cards in the grid have enhanced neon glow box-shadows that pulse on hover"
    - "The reveal screen title 'The TRUE Twitter Celebrity!' displays a glitch effect"
    - "CTA buttons on the reveal screen have neon glow on hover"
    - "Glitch animations respect prefers-reduced-motion by disabling when set to reduce"
  artifacts:
    - path: "app/styles/app.css"
      provides: "Glitch keyframes, neon glow utilities, prefers-reduced-motion guard"
      contains: "@keyframes glitch"
    - path: "app/components/StickyHeader.tsx"
      provides: "Glitch effect on header title"
      contains: "glitch"
    - path: "app/components/CelebrityCard.tsx"
      provides: "Enhanced neon glow on cards"
      contains: "neon-glow"
    - path: "app/routes/home.tsx"
      provides: "Glitch on reveal title, glow on CTAs"
      contains: "glitch"
  key_links:
    - from: "app/styles/app.css"
      to: "app/components/StickyHeader.tsx"
      via: "CSS class application"
      pattern: "glitch-text"
    - from: "app/styles/app.css"
      to: "app/components/CelebrityCard.tsx"
      via: "CSS class application"
      pattern: "neon-glow"
---

<objective>
Add cyberpunk CSS glitch effects to key text elements and enhance neon glow box-shadows on cards and interactive elements across the app.

Purpose: This is the visual polish layer that transforms the experience from "functional card game" to "cyberpunk-themed experience." Glitch effects on text and neon glow on interactive elements are the two signature cyberpunk differentiators.
Output: Updated CSS with glitch keyframes and neon glow utilities, applied to StickyHeader, CelebrityCard, and reveal screen.
</objective>

<execution_context>
@/Users/chad.cluff/personal/twittercelebrity/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chad.cluff/personal/twittercelebrity/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@app/styles/app.css
@app/components/StickyHeader.tsx
@app/components/CelebrityCard.tsx
@app/routes/home.tsx

<interfaces>
<!-- Current theme tokens in app.css (Tailwind v4 @theme): -->
<!-- --color-neon-cyan: #00f5ff -->
<!-- --color-neon-pink: #ff006e -->
<!-- --color-neon-yellow: #f5e642 -->
<!-- --color-neon-purple: #bf00ff -->
<!-- --font-cyber: "Rajdhani", "Orbitron", ui-monospace, monospace -->

<!-- CelebrityCard already has some glow: shadow-[0_0_8px_rgba(0,245,255,0.15)] -->
<!-- StickyHeader has no effects — just plain text -->
<!-- Reveal title in home.tsx uses drop-shadow but no glitch -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CSS glitch keyframes, neon glow utilities, and prefers-reduced-motion guard to app.css</name>
  <files>app/styles/app.css</files>
  <read_first>app/styles/app.css</read_first>
  <action>
Add the following CSS after the existing `body` block in app/styles/app.css:

1. **Glitch text effect** — Create a `.glitch-text` class that uses `::before` and `::after` pseudo-elements with `position: absolute`, same content via `attr(data-text)`, offset with `clip-path: inset()` keyframes. The `::before` pseudo shifts left 2px in neon-cyan (#00f5ff), the `::after` shifts right 2px in neon-pink (#ff006e). Keyframes use `steps(2, end)` for stuttery digital feel. Animation: `glitch 3s infinite` — subtle, not aggressive. The element needs `position: relative` and `display: inline-block`.

2. **Glitch keyframes** — Two keyframes: `@keyframes glitch-before` and `@keyframes glitch-after`. Each cycles through 4-5 `clip-path: inset(Y1% 0 Y2% 0)` values at different percentages (0%, 20%, 40%, 60%, 80%, 100%) to create scanning line effect. Duration 3s, infinite, with steps timing.

3. **Neon glow card utility** — Create `.neon-glow-card` class:
   - Default: `box-shadow: 0 0 8px rgba(0, 245, 255, 0.2), 0 0 20px rgba(0, 245, 255, 0.05);`
   - Hover: `box-shadow: 0 0 15px rgba(0, 245, 255, 0.4), 0 0 40px rgba(0, 245, 255, 0.15), 0 0 80px rgba(0, 245, 255, 0.05);`
   - Transition: `transition: box-shadow 0.3s ease;`

4. **Neon glow CTA utility** — Create `.neon-glow-cta` class with similar shadow pattern but using the button's own color (use CSS custom property `--glow-color` defaulting to neon-cyan). Hover intensifies the glow.

5. **prefers-reduced-motion guard** — Wrap ALL glitch keyframe animations in `@media (prefers-reduced-motion: reduce)` that sets `animation: none !important` on `.glitch-text`, `.glitch-text::before`, `.glitch-text::after`. Also disable the neon glow hover transition: set `transition: none`.

Keep the existing @import, @theme, and body blocks untouched. Only append new CSS below.
  </action>
  <verify>
    <automated>cd /Users/chad.cluff/personal/twittercelebrity && grep -c "@keyframes" app/styles/app.css | grep -q "[2-9]" && grep -q "glitch-text" app/styles/app.css && grep -q "neon-glow-card" app/styles/app.css && grep -q "prefers-reduced-motion" app/styles/app.css && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - app.css contains at least 2 @keyframes declarations (glitch-before, glitch-after)
    - app.css contains .glitch-text class with ::before and ::after pseudo-elements
    - app.css contains .neon-glow-card class with box-shadow
    - app.css contains .neon-glow-cta class with --glow-color custom property
    - app.css contains @media (prefers-reduced-motion: reduce) block that disables glitch animations
    - Existing @theme tokens and body styles are unchanged
  </acceptance_criteria>
  <done>CSS file contains glitch keyframes, neon glow utilities, and reduced-motion guard. All new — no existing styles modified.</done>
</task>

<task type="auto">
  <name>Task 2: Apply glitch effect to StickyHeader and CelebrityCard neon glow, plus reveal screen enhancements in home.tsx</name>
  <files>app/components/StickyHeader.tsx, app/components/CelebrityCard.tsx, app/routes/home.tsx</files>
  <read_first>app/components/StickyHeader.tsx, app/components/CelebrityCard.tsx, app/routes/home.tsx</read_first>
  <action>
**StickyHeader.tsx:**
- Add `data-text="Twitter Celebrity"` attribute to the h1 element (needed for the glitch pseudo-element content via `attr(data-text)`)
- Add `glitch-text` to the h1's className (alongside existing classes)
- No other changes to the component

**CelebrityCard.tsx:**
- In the outer `motion.div` className, replace the existing default (non-marked) shadow classes:
  - OLD: `"shadow-[0_0_8px_rgba(0,245,255,0.15)] hover:shadow-[0_0_16px_rgba(0,245,255,0.35)]"`
  - NEW: `"neon-glow-card"`
- Keep the isMarked red shadow unchanged (it's a game mechanic, not decoration)
- Keep the chad yellow border and non-chad cyan border logic unchanged
- Keep `transition-shadow duration-200` as it still applies to the marked/unmarked toggle

**home.tsx — Reveal screen title:**
- On the `<motion.h2>` that contains "The TRUE Twitter Celebrity!", add:
  - `data-text="The TRUE Twitter Celebrity!"`
  - Add `glitch-text` to its className
  - Keep existing drop-shadow and other classes

**home.tsx — CTA buttons:**
- On the Follow @chadcluff `<a>` tag: add `neon-glow-cta` to className, add inline style `--glow-color: rgba(0, 245, 255, 0.5)` (cyan). Remove the existing `hover:shadow-[...]` class since neon-glow-cta handles it.
- On the Share on X `<a>` tag: add `neon-glow-cta` to className, add inline style `--glow-color: rgba(255, 0, 110, 0.5)` (pink). Remove the existing `hover:shadow-[...]` class.
- On the Play Again `<button>`: add `neon-glow-cta` to className, add inline style `--glow-color: rgba(245, 230, 66, 0.5)` (yellow). Keep existing hover bg/text color change — the glow is additive.

Do NOT change any animation logic, state management, or component structure. CSS class changes only.
  </action>
  <verify>
    <automated>cd /Users/chad.cluff/personal/twittercelebrity && grep -q 'glitch-text' app/components/StickyHeader.tsx && grep -q 'data-text' app/components/StickyHeader.tsx && grep -q 'neon-glow-card' app/components/CelebrityCard.tsx && grep -q 'glitch-text' app/routes/home.tsx && grep -q 'neon-glow-cta' app/routes/home.tsx && npx react-router build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - StickyHeader h1 has both `glitch-text` class and `data-text` attribute
    - CelebrityCard uses `neon-glow-card` class instead of inline shadow utilities
    - home.tsx reveal title has `glitch-text` class and `data-text` attribute
    - home.tsx Follow CTA has `neon-glow-cta` class
    - home.tsx Share CTA has `neon-glow-cta` class
    - home.tsx Play Again button has `neon-glow-cta` class
    - Build succeeds without errors
  </acceptance_criteria>
  <done>Glitch effects visible on header and reveal title. Enhanced neon glow on all cards and CTA buttons. Build passes. No functional changes to game logic.</done>
</task>

</tasks>

<verification>
1. `npx react-router build` succeeds without errors
2. `grep -c "glitch-text" app/components/StickyHeader.tsx` returns >= 1
3. `grep -c "neon-glow-card" app/components/CelebrityCard.tsx` returns >= 1
4. `grep -c "glitch-text" app/routes/home.tsx` returns >= 1
5. `grep -c "neon-glow-cta" app/routes/home.tsx` returns >= 3
6. `grep -c "prefers-reduced-motion" app/styles/app.css` returns >= 1
</verification>

<success_criteria>
- CSS glitch effect animates on "Twitter Celebrity" header text and "The TRUE Twitter Celebrity!" reveal title
- Cards display enhanced neon glow that intensifies on hover
- CTA buttons glow with their respective neon colors on hover
- All glitch animations disabled when prefers-reduced-motion: reduce is active
- Build produces no errors
- No changes to game state, animation timing, or component logic
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-and-launch/04-01-SUMMARY.md`
</output>
